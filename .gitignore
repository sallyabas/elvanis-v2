# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.local.example

# vercel
.vercel

# Claude Code local settings — this session's permission allowlist ended up
# recording secrets inline (Supabase access token, DB password, Groq key)
# via Bash command strings. Never commit this file.
.claude/settings.local.json

# Supabase CLI local link cache — machine-specific, regenerated by `supabase link`
supabase/.temp/

# typescript
*.tsbuildinfo
next-env.d.ts
