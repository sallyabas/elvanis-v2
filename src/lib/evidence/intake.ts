// Per-lens evidence intake: native export upload, fill-in template, or both
// merged via the merge-and-complete step. See spec §2.3 steps 2-3. Phase 2 work.

export async function createEvidenceSubmission(): Promise<never> {
  throw new Error("createEvidenceSubmission: not yet implemented (Phase 2)");
}
